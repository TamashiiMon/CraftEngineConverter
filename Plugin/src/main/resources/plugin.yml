name: ${project.parent.artifactId}
version: ${plugin.finalVersion}
authors: [ ${plugin.author} ]
description: ${plugin.description}
main: ${plugin.mainClass}
api-version: ${plugin.apiVersion}
softdepend:
  - CraftEngine
  - packetevents
  - PlaceholderAPI
folia-supported: true